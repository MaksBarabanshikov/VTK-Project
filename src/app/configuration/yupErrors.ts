const yupErrors = {
  required: 'Required',
  email: 'Please enter a valid email address',
  confirmPassword: 'Passwords don\'t match',
};

export const { confirmPassword, email, required } = yupErrors;